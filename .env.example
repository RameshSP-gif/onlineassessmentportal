# Server Configuration
PORT=3002
NODE_ENV=development

# React App API URL
REACT_APP_API_URL=http://localhost:3002/api

# JWT Configuration
JWT_SECRET=secret_key_123

# MongoDB Atlas
MONGODB_URI=mongodb+srv://root:Rakshita%401234@cluster0.pp8rwbt.mongodb.net/assessmentdb?retryWrites=true&w=majority

# Email Configuration (Optional - Development uses Ethereal Email by default)
# For Production: Set these to use Gmail or custom SMTP
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password

# Note: In development mode, OTP emails are sent via Ethereal Email (test service)
# The OTP will be logged to the server console
# To view the email, use the preview URL returned by the API
